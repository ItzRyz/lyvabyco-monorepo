{
  "version": 2,
  "installCommand": "cd ../../ && bun install",
  "buildCommand": "cd ../../ && bunx turbo run build --filter=api",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index"
    }
  ]
}
